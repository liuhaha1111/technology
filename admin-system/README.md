# Admin System

Independent admin platform with isolated frontend and backend.

## Workspaces

- `admin-backend`: Express + TypeScript API service
- `admin-frontend`: React + Vite + TypeScript web app

## Quick Start

```bash
npm --prefix admin-system install
npm --prefix admin-system run dev
```
